# Google 2FA Android

A local Android TOTP authenticator supporting multiple accounts.

## Features

- Add and delete multiple accounts
- Generate 6-digit TOTP codes every 30 seconds
- Tap a code to copy it
- Store account data locally on the device

## Build

Run `build-apk.ps1` on Windows, or use the included GitHub Actions workflow.
